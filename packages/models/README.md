# @aegis/models

**Purpose** — Shared, domain-agnostic primitives and the common Entity base shape. No single SDK owns these (ADR 0002).

**Public surface** — `Money`/`MoneySchema`, `CurrencyCode`, `EntityType`, `EntityRef`, `EntityBase` (+ Zod schemas).

**Dependencies** — `zod` only.

**Status** — live.
