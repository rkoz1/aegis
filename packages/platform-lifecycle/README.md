# @aegis/platform-lifecycle

**Purpose** — The Investment Lifecycle spine: the nine ordered, front-to-back-office stages and `groupByStage`, used to curate the Navigation Taxonomy and the Lifecycle Map. Editorial ordering, distinct from the Registry.

**Public surface** — `LIFECYCLE_STAGES`, `LifecycleStage`, `groupByStage`, `StageGroup`.

**Dependencies** — none.

**Tests** — `lifecycle.test.ts` (order, grouping).

**Status** — live.
