# @aegis/function-portfolio-snapshot

**Purpose** — Displays a snapshot of portfolios. The Phase 0 tracer-bullet Function proving the Function → SDK → mock-data → UI thread.

**Public surface** — `portfolioSnapshotManifest` (FunctionManifest), `PortfolioSnapshot` (component). Route: `/portfolio-snapshot`.

**Context consumed/emitted** — None yet (Context Bus arrives in Phase 3).

**SDK dependencies** — `@aegis/sdk-portfolios` (`usePortfolios`).

**Status** — live (minimal; representative Function for the spine).
